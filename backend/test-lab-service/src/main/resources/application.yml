server:
  port: 9006

spring:
  application:
    name: TEST-LAB-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://bc.spcluster.tk/eureka/
  instance:
    hostname: bc.spcluster.tk
