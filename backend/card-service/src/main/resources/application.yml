server:
  port: 9003

spring:
  application:
    name: CARD-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://serviceregistry.bc61.tk/eureka/
  instance:
    hostname: serviceregistry.bc61.tk
